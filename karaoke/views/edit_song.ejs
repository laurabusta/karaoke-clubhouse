<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css"> 
    <title>Edit Song</title>
</head>
<body>
    <div class="container">
        <h1>Edit This Groovy Tune</h1>
        <!-- BACK TO PROFILE BUTTON, would prefer this to be a close button -->
        <button onclick="location.href='/lobby/<%= member.id %>'" class="button-nav">Back to Profile</button>
        <!-- form for new song data -->
        <form action="/song/<%= song.id %>/<%= member.id %>?_method=PUT" method="POST" class="form-page">
            <!-- artist text input -->
            <div>
                <label for="artist">Artist:</label>
                <input type="text" name="artist" value="<%= song.artist %>">
            </div>
            <!-- title text input -->
            <div>
                <label for="title">Title:</label>
                <input type="text" name="title" value="<%= song.title %>">
            </div>
            <!-- genre text input -->
            <div>
                <label for="genre">Genre:</label>
                <input type="text" name="genre" value="<%= song.genre %>">
            </div>
            <!-- songId text input -->
            <div>
                <label for="songId">Song ID:</label>
                <input type="text" name="songId" value="<%= song.songId %>">
            </div>
            <!-- form submit button -->
            <input type="submit" value ="Update Song" class="button-submit">
        </form>
    </div>
</body>
</html>